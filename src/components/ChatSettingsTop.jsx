import React from 'react'

import NavBar from './NavBar';

function ChatSettingsTop(props) {
    const {creds, chat} = props
  
    return (
        <NavBar/>
     
    )
}

export default ChatSettingsTop
